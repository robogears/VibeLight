# What's new in v0.1.6

## A proper drag-to-Applications installer
- Fresh installs now come as a familiar **DMG**: open it and **drag VibeLight
  into the Applications folder**, with an on-brand background and arrow. Belt
  and suspenders with the in-app "Move to Applications" prompt from v0.1.5.

## Also in recent updates
- **Move to Applications** on first launch (v0.1.5), and the self-updater's
  **Update Now → download → Restart Now** flow (v0.1.4) — if you're on v0.1.4+
  this update should have arrived automatically. 🎉

---

# Install / update

- **On v0.1.4 or later?** Click **Update Now** when offered (or Settings → About
  → Software Update), then **Restart Now**. No download needed.
- **Fresh install (macOS, Apple Silicon):** download `VibeLight-0.1.6-arm64.dmg`,
  open it, and **drag VibeLight into the Applications folder**. First launch on
  macOS 15+: right-click → Open, or **System Settings → Privacy & Security →
  Open Anyway** (the app is not notarized).
  *(A `-arm64.zip` is also attached — it's what the in-app updater uses.)*
- On first stream, macOS may ask to allow **VibeLight Stream** on the local
  network — click Allow.

## Requirements

- Apple Silicon Mac, macOS 15 or later.
- [Moonlight](https://moonlight-stream.org) installed and already paired with
  your host once (VibeLight imports that pairing).
- A Sunshine-family host (Sunshine / Apollo / Vibepollo) on the gaming PC.

---

**Full Changelog**: https://github.com/robogears/VibeLight/compare/v0.1.5...v0.1.6
